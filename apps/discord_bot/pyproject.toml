[project]
name = "discord-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hikari-crescent>=1.4.0",
    "python-dotenv",
    "aiohttp",
    "wardogs-schemas",
]

[tool.uv.sources]
wardogs-schemas = { workspace = true }

[tool.pytest.ini_options]
pythonpath = ["."]
